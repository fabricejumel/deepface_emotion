# deepface_emotion

on commence par crée le venv
```bash
python3 -m venv venv_deepface_emotion_venv
```
on l'active
```bash
source venv_deepface_emotion_venv/bin/activate
```

installation des paquets
```bash
pip -r requirements_deepface.txt
```
